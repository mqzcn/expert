{
  "name": "expert-language-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "rm -rf dist && mkdir -p dist && cp -r src/* dist/ && cp package*.json dist/ && cd dist && npm install --production",
    "clean": "rm -rf dist"
  },
  "main": "src/index.js",
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "exceljs": "^4.4.0",
    "express": "^4.18.2",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.1",
    "nodemailer": "^6.9.7",
    "resend": "^2.0.0",
    "zod": "^3.22.4"
  }
}
